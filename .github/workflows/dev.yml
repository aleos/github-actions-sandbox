name: Distribute Dev

on:
  push:
    branches:
      - dev
  pull_request:
    types: [closed]

jobs:
  dev:
    uses: ./.github/workflows/distribute.yml
    with:
      scheme: "Some"
    secrets: inherit

name: Distribute Dev

on:
  pull_request:
    types: [closed]
    branches:
      - dev
  push:
    branches:
      - dev


jobs:
  dev:
    uses: ./.github/workflows/distribute.yml
    with:
      scheme: "Some"
    secrets: inherit
